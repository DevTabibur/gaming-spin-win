/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    distDir: 'build' // Custom output folder (e.g., 'build' instead of 'out')
  };
  
  export default nextConfig;
  